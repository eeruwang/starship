{
  "name": "starship",
  "version": "1.0.0",
  "description": "Multi-account Fediverse dashboard for Misskey, Iceshrimp, CherryPick, and Mastodon",
  "scripts": {
    "test": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test tests/*.test.js",
    "check": "npm run check:syntax && npm test",
    "check:syntax": "node -e \"const { execFileSync } = require('node:child_process'); for (const f of process.argv.slice(1)) execFileSync(process.execPath, ['--check', f], { stdio: 'inherit' });\" public/js/*.js public/js/api/*.js public/js/mixins/*.js public/js/ui/*.js src/*.js tests/*.js",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "deploy:staging": "npx wrangler deploy --env staging",
    "tail": "npx wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "keywords": ["fediverse", "misskey", "mastodon", "iceshrimp", "cherrypick", "cloudflare-workers"],
  "license": "MIT"
}
